package com.goreacraft.plugins.borders;





public class Methods 
{
	/*static void loadAllworlds()
	{
		
	chechfolder();
        
        for (File file2 : Main.bordersfolder.listFiles()) 
        {
		    if (file2.isFile() && file2.getName().endsWith(".yml")) 
		    {
		    	
		    	System.out.println("------------------------Checking " + file2.getName().substring(0, file2.getName().lastIndexOf(".")));

		    	loadWorldFile(file2.getName().substring(0, file2.getName().lastIndexOf(".")));
		    }
		}
	}*/



/*	static void chechfolder()
	{
	     if(!Main.bordersfolder.exists())
	     	{
	    	 Main.bordersfolder.mkdirs();
	     	}
	}
*/
	
	
	
	 /*static void checkplayerfile(String name){     	
     	File playersDataFile= new File(Main.bordersfolder, name + ".yml");
     	chechfolder();
 		if (!playersDataFile.exists())
 		{
 	            try {
 	            	playersDataFile.createNewFile();
 	            } catch (IOException e) {
 	                e.printStackTrace();
 	            }
 	          //  checkFixPlayerData(name); 
 		}
     }*/
	
}